\
    -- Example view (MySQL requires ALGORITHM and SQL SECURITY if enforced by DdlGuard)
    -- CREATE VIEW vw_example
    --   ALGORITHM=MERGE SQL SECURITY INVOKER
    -- AS SELECT id, name FROM example;
