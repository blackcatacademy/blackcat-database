<?php
declare(strict_types=1);
use PHPUnit\Framework\TestCase;
final class {{Name}}OperationResultTraceTest extends TestCase {
  public function test_correlation_and_meta(): void {
    if (!class_exists('\\OperationResult')) { $this->markTestSkipped('OperationResult not found'); }
    $r = \OperationResult::ok(['x'=>1], 'corr-123', ['retries'=>2]);
    $this->assertTrue($r->ok);
    $this->assertSame('corr-123', $r->correlationId);
    $this->assertSame(2, $r->meta['retries'] ?? null);
  }
}