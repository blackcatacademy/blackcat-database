@{
    # Example profiles for view selection (to be refined)
    profiles = @{
        full = @{
            tags = @('core')
        }
        minimal = @{
            tags = @()
        }
        analytics = @{
            tags = @('analytics','reporting')
        }
    }
}
